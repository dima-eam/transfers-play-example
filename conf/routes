# Creates user with internal account
POST     /transfers/api/createUser                 controllers.user.UserController.create

# Deposit user account by email
POST     /transfers/api/depositByEmail   controllers.transfers.TransfersController.depositByEmail

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)